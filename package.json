{
  "name": "chrome-extension-typescript-template",
  "private": true,
  "version": "1.0.0",
  "description": "A TypeScript Chrome/Edge extension template built with Vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && powershell -ExecutionPolicy Bypass -File fix-build.ps1",
    "build:watch": "vite build --watch",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist",
    "zip": "npm run build && cd dist && powershell Compress-Archive -Path * -DestinationPath ../extension.zip -Force"
  },
  "keywords": [
    "chrome-extension",
    "typescript",
    "vite",
    "browser-extension",
    "template"
  ],
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "@types/webextension-polyfill": "^0.12.3",
    "rimraf": "^6.0.1",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vite-plugin-web-extension": "^4.4.5",
    "webextension-polyfill": "^0.12.0"
  }
}
